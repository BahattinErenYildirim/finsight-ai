# ── requirements.txt ──────────────────────────────────────────────────────────
# FinSight AI — BIST Yapay Zeka Finansal Analiz Platformu
# Kurulum: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Veri & analiz (çekirdek) ─────────────────────────────────────────────────
yfinance>=0.2.31
curl_cffi>=0.6.0
certifi>=2024.0.0
pandas>=2.0.0
numpy>=1.24.0
ta>=0.11.0

# LLM (Gemini API)
google-genai>=1.0.0

# Konfigürasyon
python-dotenv>=1.0.0

# Web scraping (haber desteği)
requests>=2.31.0
beautifulsoup4>=4.12.0

# CLI arayüzü
rich>=13.0.0

# Web dashboard
streamlit>=1.30.0
plotly>=5.18.0

# PDF rapor üretimi
fpdf2>=2.8.0
kaleido>=1.0.0

# ── Backend API (FastAPI) ────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
slowapi>=0.1.9
email-validator>=2.0.0
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0
# PostgreSQL için (üretim): pip install asyncpg
asyncpg>=0.29.0
redis>=5.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<4.1.0
prometheus-fastapi-instrumentator>=6.0.0
sentry-sdk[fastapi]>=1.40.0
apscheduler>=3.10.0

# ── Testler ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
httpx>=0.26.0
ruff>=0.4.0
